{
  "noInstances": {
      "name": "demo-api",
      "dockerRepository": "lukebond/demo-api",
      "config": {
        "publicFacing": false,
        "ports": [],
        "env": {}
      }
  },
  "noConfig": {
      "name": "demo-api",
      "dockerRepository": "lukebond/demo-api"
  },
  "oneInstance": {
    "name": "demo-api",
    "dockerRepository": "lukebond/demo-api",
    "config": {
      "publicFacing": false,
      "numInstances": 1,
      "ports": [],
      "env": {}
    }
  },
  "twoInstances": {
    "name": "demo-api",
    "dockerRepository": "lukebond/demo-api",
    "config": {
      "publicFacing": false,
      "numInstances": 2,
      "ports": [],
      "env": {}
    }
  },
  "threeInstances": {
    "name": "demo-api",
    "dockerRepository": "lukebond/demo-api",
    "config": {
      "publicFacing": false,
      "numInstances": 3,
      "ports": [],
      "env": {}
    }
  }
}
