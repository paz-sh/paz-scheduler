{
  "noInstances": {
      "name": "demo-api",
      "dockerRepository": "lukebond/demo-api",
      "config": {
        "publicFacing": false,
        "ports": [],
        "env": {},
        "user": "core"
      }
  },
  "noConfig": {
    "name": "demo-api",
    "dockerRepository": "lukebond/demo-api"
  },
  "oneInstance": {
    "name": "demo-api",
    "dockerRepository": "lukebond/demo-api",
    "config": {
      "publicFacing": false,
      "numInstances": 1,
      "ports": [],
      "env": {},
      "user": "core"
    }
  },
  "twoInstances": {
    "name": "demo-api",
    "dockerRepository": "lukebond/demo-api",
    "config": {
      "publicFacing": false,
      "numInstances": 2,
      "ports": [],
      "env": {},
      "user": "core"
    }
  },
  "threeInstances": {
    "name": "demo-api",
    "dockerRepository": "lukebond/demo-api",
    "config": {
      "publicFacing": false,
      "numInstances": 3,
      "ports": [],
      "env": {},
      "user": "core"
    }
  }
}
